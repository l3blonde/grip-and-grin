logout"
login"
register"
mobile-only"
active"
logout"
login"
register"
active"
secondary"
primary"
secondary"
role-{{
user.role.value
}}"
status-{{
user.isActive
?
'active'
:
'inactive'
}}"
view"
edit"
secondary"
secondary"
primary"
secondary"
primary"
status-{{
article.status.value
}}"
view"
edit"
delete"
pagination-prev"
pagination-next"
primary"
role-badge
role-{{
session.currentUserRole
}}"
pagination-prev"
pagination-next"
observe-lazy"
lazy-image"
pagination-prev"
pagination-next"
secondary"
readonly"
role-{{
user.role.value
}}"
primary"
secondary"
role-{{
user.role.value
}}"
primary"
secondary"
role-{{
user.role.value
}}"
pagination-prev"
pagination-next"
